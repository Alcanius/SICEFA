# SICEFA

This repository contains a minimal example of a Laravel 8 CRUD application located in the `crudapp` folder.

To set up the application, read `crudapp/README.md` and follow the installation steps using Composer and PHP.
